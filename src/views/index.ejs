<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>블로그 홈</title>
    <!-- Bootstrap CSS -->
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@400;700&display=swap" rel="stylesheet">
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body, h1, h2, h3, h4, h5, h6, p, a {
            font-family: 'Noto Sans KR', sans-serif;
        }

        a {
            color: inherit;
            text-decoration: none;
        }

        a:hover {
            text-decoration: underline;
        }

        .post-time {
            font-size: 0.8rem;  /* 글자 크기를 작게 조절 */
            float: right;      /* 오른쪽으로 배치 */
            margin-right: 10px;/* 오른쪽 마진 추가 (필요에 따라 조절 가능) */
        }
    </style>
    <script>
        function handleCredentialResponse(googleUser) {
            console.log('user: ' + JSON.stringify(googleUser)); // Do not send to your backend! Use an ID token instead.
            // 코드가 존재할 경우 서버로 전송
            const code = googleUser.credential
            if (code) {
                localStorage.setItem('google_auth_code', code);

                fetch(`/auth/callback?code=${code}`, {
                    method: 'GET',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                })
                    .then(response => response.json())
                    .then(data => {
                        console.log('서버 응답:', data);
                    })
                    .catch(error => {
                        console.error('서버 요청 중 오류 발생:', error);
                    });
            } else {
                console.error('Google 인증 응답에서 code를 찾을 수 없습니다.');
            }

        }


        // 페이지 로드 시 실행되는 함수
        document.addEventListener('DOMContentLoaded', function() {
            const savedCode = localStorage.getItem('google_auth_code');
            if (savedCode) {
                fetch(`/auth/callback?code=${savedCode}`, {
                    method: 'GET',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                })
                    .then(response => {
                        if (!response.ok) {
                            throw new Error('서버 로그인 실패');
                        }
                        return response.json();
                    })
                    .then(data => {
                        console.log('서버 응답:', data);
                    })
                    .catch(error => {
                        console.error('서버 요청 중 오류 발생:', error);
                        // 로그인 시도 중 에러 발생 시 로컬 스토리지의 code 삭제
                        localStorage.removeItem('google_auth_code');
                    });
            }
        });
    </script>
</head>

<body>
<div class="container mt-5">
    <!-- Homepage Title -->
    <div class="text-center mb-5">
        <h1>오늘 하루를 남겨보세요</h1>
        <p>일상에 관한 나의 이야기</p>
    </div>

    <a href="chohh">조환희 님의 블로그</a>


    <script src="https://accounts.google.com/gsi/client" async defer></script>
    <div id="g_id_onload"
         data-client_id="125703249510-5ska8h67gdtojna23e4enr5iqvrl9alc.apps.googleusercontent.com"
         data-callback="handleCredentialResponse">
    </div>
    <div class="g_id_signin" data-type="icon" data-text="signup_with"></div>
    <a href="#" onclick="signOut();">Sign out</a>

</div>

<!-- Optional: jQuery and Bootstrap JS -->
<script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.3/dist/umd/popper.min.js"></script>
<script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>

</html>
