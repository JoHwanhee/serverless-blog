<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= post.getTitle() %></title>

    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@400;700&display=swap" rel="stylesheet">

    <style>
        body, h1, h2, h3, h4, h5, h6, p, a {
            font-family: 'Noto Sans KR', sans-serif;
        }

        .navbar {
            margin-bottom: 2rem;
        }

        .post-date {
            font-size: 0.9rem;
            color: gray;
        }
    </style>
</head>

<body>
<div class="container mt-5">
    <!-- Navbar for Back Button -->
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand" href="/"><i class="fas fa-arrow-left"></i> 뒤로가기</a>
    </nav>

    <!-- Post Content -->
    <div class="mt-4">
        <h2 class="mb-2"><%= post.getTitle() %></h2>
        <p class="post-date">작성일: <%= post.getFormattedCreatedAt() %></p> <!-- Date format: yyyy/mm/dd -->
        <% if(post.isThumbnailDisplayable()) { %>
            <img class="img-fluid mb-3" src="<%= post.getThumbnailUrl() %>" alt="<%= post.getTitle() %>의 썸네일">
        <% } %>
        <p class="text-muted"><i><%= post.getDescription() %></i></p>
        <hr>
        <p class="post-content"><%= post.getContent() %></p>
    </div>
</div>

<script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.3/dist/umd/popper.min.js"></script>
<script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
<!-- FontAwesome for icons -->
<script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>
</body>
</html>
